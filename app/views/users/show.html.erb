<h1 id="username" class="userName" data-id="<%= @user.id %>"><%= @user.name %>'s Stats </h1>

<table class="table table-hover" id="user-games">
	
		
</table>
	

<% if current_user_page(current_user) %>
  <% if current_user.matches.count >= 1  %>
    <p>Your favorite game is <%= @fav %>. 
  <% end %>
<% end %>

<h3><%= current_user_page(current_user) %></h3>
<div id="new-game-form"></div>
