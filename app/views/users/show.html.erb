<a href="#" class="userName" data-id="<%= @user.id %>"><h1 id="username" ><%= @user.name %>'s Stats </h1></a>

<p id="user-games"></p>
<table class="table table-hover">
	<thead>
		<tr>
			<th scope="col">Date Played</th>
			<th scope="col">Won?</th>
		</tr>
	</thead>
	<tbody id="user-matches">
	</tbody>
</table>
























<% if current_user_page(current_user) %>
  <% if current_user.matches.count >= 1  %>
    <p>Your favorite game is <%= @fav %>. 
  <% end %>
<% end %>

<h3><%= current_user_page(current_user) %></h3>
